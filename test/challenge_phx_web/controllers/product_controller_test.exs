defmodule ChallengePhxWeb.ProductControllerTest do
  use ChallengePhxWeb.ConnCase
  # Import Hound helpers
  use Hound.Helpers

  # Start a Hound session
  hound_session

  # test "GET /" do
  #   navigate_to "/"
  #   assert page_source =~ "Welcome to Phoenix"
  # end
  # test "GET /", %{conn: conn} do
  #   conn = get conn, "/"
  #   assert html_response(conn, 200) =~ "Welcome to Phoenix!"
  # end
end
